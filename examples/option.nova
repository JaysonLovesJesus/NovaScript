enum Option<T> {
  Some(T),
  None
}

fn find(arr: num[], target: num): Option<num> {
  let mut i = 0;
  while (i < arr.length) {
    if (arr[i] == target) {
      Option_Some(i)
    }
    i + 1
  }
  Option_None
}
